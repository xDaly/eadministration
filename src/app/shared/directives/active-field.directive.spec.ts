import { ActiveFieldDirective } from './active-field.directive';

describe('ActiveFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new ActiveFieldDirective();
    expect(directive).toBeTruthy();
  });
});
